import {
  border,
  borderRadius,
  breakpoints,
  colors,
  shadows,
  spacing,
  typography,
  zIndex,
} from '@monorepo-frontend/styles/tokens';

export function useTheme() {
  return {
    border,
    borderRadius,
    breakpoints,
    colors,
    shadows,
    spacing,
    typography,
    zIndex,
  };
}
